version: '3.8'

services:
  docx-generator:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
    restart: unless-stopped
